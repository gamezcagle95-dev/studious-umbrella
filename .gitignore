# Python Virtual Environment and Cache
venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/

# Node Dependencies and Build Artifacts
node_modules/
artifacts/
cache/
typechain/
typechain-types/
out/
.openzeppelin/

# Environment Variables and Sensitive Configs
.env
.env.local
.env.*.local
*.pem
*.key
private_key.json

# Local Hashing Pipeline Artifacts
# (We exclude local proof packets to keep the public repo clean)
artifacts/proof_packet.json
artifacts/proofs/

# Raw training interaction datasets and system weights
data/datasets/
data/weights/
